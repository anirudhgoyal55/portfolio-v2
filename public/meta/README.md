# Hero screenshot slot

Drop a screenshot here as `hero.png` (or `.jpg`). Recommended size 1600×900
(2× display) showing the home page. The README references this image
right under the title.

To capture: open the deployed site at the home page in dark or light mode
(matching the brand), full-window, and save as `hero.png`.
