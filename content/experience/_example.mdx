---
company: "Replace with your company"
role: "Replace with your role"
startDate: "2024-01"
endDate: "present"
location: "Remote"
url: ""
order: 1
draft: true
---

This is a template entry. Drop a new `.mdx` file in `content/experience/` for each role on your timeline. Set `draft: true` to hide an entry. Set `endDate: "present"` for current roles. Order is descending by `startDate` automatically; `order` is a tiebreaker.

The body is the role description — what you owned, what shipped, what you'd write more about. MDX, so links and emphasis work.
